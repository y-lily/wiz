import pygame as pg

USE = (pg.K_SPACE,
       )

UP = (pg.K_UP,
      pg.K_KP_8,
      )

DOWN = (pg.K_DOWN,
        pg.K_KP_2,
        )

LEFT = (pg.K_LEFT,
        pg.K_KP_4,
        )

RIGHT = (pg.K_RIGHT,
         pg.K_KP_6,
         )

ESCAPE = (pg.K_ESCAPE,
          )

ZOOM_OUT = (pg.K_MINUS,
            pg.K_KP_MINUS,
            )

ZOOM_IN = (pg.K_EQUALS,
           pg.K_KP_PLUS,
           )

SEND = (pg.K_RETURN,
        pg.K_KP_ENTER,
        )
